dind:
  title: "Dind"
  icon: "anchor"
  init: false
  tooltip: "Docker in Docker Orchestrator"
  force_restart: true
  config: null  
  tags:
    - orchestrator
    - docker
  services: # Each workflow can run multiple services or commands
    - dind
